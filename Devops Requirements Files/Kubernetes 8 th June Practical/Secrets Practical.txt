Follow steps in book all same until 1-2-3.


4) Go to the role we created for the node -> delete the EC2 container, elastic container policy.

5) kubectl create secret docker-registry regcred \                 ----? edit account id and region
  --docker-server=138250642287.dkr.ecr.ap-south-1.amazonaws.com \
  --docker-username=AWS \
  --docker-password=$(aws ecr get-login-password) 

6) kubectl create deployment mydep --image 138250642287.dkr.ecr.ap-south-1.amazonaws.com/secrets-repo:latest --replicas 2   ----> wanted to create a pod using the image so we copy pasted the 4th command from the view push commands

   - 138250642287.dkr.ecr.ap-south-1.amazonaws.com/secrets-repo:latest ----> get this command from the repo's view push commands 4th command

7) kubectl get pods  ----> u should get errorimagepulloff


8) delete previously created deployment group

9) kubectl create deployment mydep --image 138250642287.dkr.ecr.ap-south-1.amazonaws.com/secrets-repo:latest --replicas 2 --dry-run -o yaml

10) nano secretdep.yml ----> copy paste the entire code given after dry run
    apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: mydep
  name: mydep
spec:
  replicas: 2
  selector:
    matchLabels:
      app: mydep
  strategy: {}
  template:
    metadata:
      labels:
        app: mydep
    spec:
      containers:
      - image: 138250642287.dkr.ecr.ap-south-1.amazonaws.com/secrets-repo:latest
        name: secrets-repo
      imagePullSecrets:
      - name: regcred

11) kubectl get secrets  ----> gives the name of secret 


12) kubectl apply -f secretdep.yml

13) kubectl get pods            -----> pods should be upend running 